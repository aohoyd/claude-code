---
description: Create a new empty jj changeset
allowed-tools: Bash(jj:*)
---

Create a new empty changeset on top of the current one.

Run `jj new` to create a fresh changeset.

After creation, run `jj log -r @::` to show the new changeset in context.
